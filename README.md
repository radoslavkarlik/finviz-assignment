Frontend repository setup via `vite create` command.
Switched to `pnpm` as a package manager due to preference and smaller dependency footprint.

Tailwind for styling

Eslint for lint
Oxfmt for format (Fast alternative to prettier/eslint)

Using Docker for consistent local development environment.

Start
docker compose up
